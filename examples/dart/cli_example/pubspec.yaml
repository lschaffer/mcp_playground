name: mcp_cli_example
description: Interactive CLI for mcp_playground_dart — chat with LLMs using MCP tools.
version: 1.0.0
publish_to: 'none'

environment:
  sdk: ^3.12.0

dependencies:
  mcp_playground_dart:
    path: ../../../mcp_playground_dart
  yaml: ^3.1.2

dev_dependencies:
  lints: ^6.0.0
