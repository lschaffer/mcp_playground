name: mcp_playground_shared_dart_tools
description: Pure-Dart MCP tool implementations (weather, chart, SSH) shared across examples.
version: 1.0.0
publish_to: 'none'

environment:
  sdk: ^3.12.0

dependencies:
  mcp_playground_dart:
    path: ../../mcp_playground_dart
  http: ^1.2.1
